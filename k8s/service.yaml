apiVersion: v1
kind: Service
metadata:
  name: fte-api-service
  namespace: customer-success-fte
  labels:
    app: fte-api
spec:
  selector:
    app: fte-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
